services:
  worker:
    build: ./lb-demo/worker
    deploy:
      replicas: 3
    expose:
      - "8000"

  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - worker
